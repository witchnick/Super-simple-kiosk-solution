services:
  web:
    image: php:8.2-apache
    container_name: kiosk
    ports:
      - "8080:80"
    volumes:
      - .:/var/www/html
      - ./php.ini:/usr/local/etc/php/php.ini
    command: >
      bash -c "chown -R www-data:www-data /var/www/html/uploads && apache2-foreground"
